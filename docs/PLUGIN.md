# Beads Claude Code Plugin

This guide moved to [integrations/claude-code-plugin.md](integrations/claude-code-plugin.md)
as part of the Mintlify docs site under `docs/`. This pointer stays because
released `bd` binaries reference `docs/PLUGIN.md` (for example in `bd doctor`
fix suggestions).
