# JSON Output Schema Contract

This reference moved to [reference/json-schema.md](reference/json-schema.md) as
part of the Mintlify docs site under `docs/`. This pointer stays because
released `bd` binaries reference `docs/JSON_SCHEMA.md` in schema-migration
messages.
