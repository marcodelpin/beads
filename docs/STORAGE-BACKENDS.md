# Storage Backends

This guide moved to [architecture/storage-backends.md](architecture/storage-backends.md)
as part of the Mintlify docs site under `docs/`. This pointer stays because
released `bd` binaries reference `docs/STORAGE-BACKENDS.md` (for example in
`bd init --backend` help).
